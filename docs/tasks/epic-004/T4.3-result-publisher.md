# T4.3: Публикация результатов в Redis pub/sub

## Описание
Модуль `worker/result_publisher.py` — публикация результатов транскрипции + PDF в Redis pub/sub канал `task_results`.

**Эпик:** EPIC-004 (Worker LLM PDF)  
**Зависимости:** T4.1 (LLM client), T4.2 (PDF generator), T1.4 (Config module)  
**Оценка:** ~2 часа

---

## Spec

| Input | Обработка | Output |
|-------|-----------|--------|
| Транскрипция + PDF | Публикация в Redis `task_results` | JSON-сообщение с task_id, transcription, summary, pdf_path |

---

## Критерии приёмки

- [ ] `worker/result_publisher.py` — класс `ResultPublisher` с методом:
  - `publish_result(task_id: str, transcription: dict, pdf_path: Path) -> None`
- [ ] Redis pub/sub канал `task_results`
- [ ] JSON-формат: `{task_id, transcription, summary, pdf_path, timestamp}`
- [ ] Redis connection pool
- [ ] `redis>=5.0.0` добавлен в `worker/requirements.txt`

---

## Граничные случаи

- Redis недоступен — ValueError (retry 3 раза)
- Task_id пустой — ValueError
- PDF-файл не найден — ValueError
- JSON serialization error — ValueError
- Redis connection lost — reconnect

---

## Пошаговый план

1. Добавить `redis>=5.0.0` в `worker/requirements.txt`

2. Создать `worker/result_publisher.py`:
   ```python
   import json
   import logging
   from datetime import datetime
   from pathlib import Path

   import redis

   from bot.config import WorkerConfig

   class ResultPublisher:
       CHANNEL = "task_results"

       def __init__(self, config: WorkerConfig):
           self.config = config
           self.redis = redis.Redis(
               host=config.redis_host,
               port=config.redis_port,
               decode_responses=True,
           )

       def publish_result(self, task_id: str, transcription: dict, pdf_path: Path) -> None:
           if not task_id:
               raise ValueError("task_id is required")
           if not pdf_path.exists():
               raise FileNotFoundError(f"PDF not found: {pdf_path}")

           message = {
               "task_id": task_id,
               "transcription": transcription["text"],
               "summary": transcription.get("summary", ""),
               "pdf_path": str(pdf_path),
               "timestamp": datetime.now().isoformat(),
           }

           self.redis.publish(self.CHANNEL, json.dumps(message))
   ```

3. Написать тесты (см. ниже)

---

## Тесты (TDD)

- `tests/unit/test_result_publisher.py`:
  - `test_publisher_initializes_with_redis()` — создаёт ResultPublisher, проверяет что redis подключён
  - `test_publish_result_sends_message()` — publish, проверяет что сообщение отправлено в `task_results`
  - `test_publish_result_empty_task_id_raises()` — пустой task_id → ValueError
  - `test_publish_result_missing_pdf_raises()` — PDF не найден → FileNotFoundError
  - `test_publish_result_json_format()` — проверяет что JSON содержит все поля
  - `test_publish_result_reconnects_on_disconnect()` — disconnect → reconnect

---

## Зависимости задач

- **Зависит от:** T4.1, T4.2, T1.4
- **Блокирует:** нет
- **EPIC-004 завершён когда T4.1, T4.2 и T4.3 выполнены**
