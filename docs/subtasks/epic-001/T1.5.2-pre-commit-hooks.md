# T1.5.2 — Create Pre-commit Hooks

## Цель
Создать pre-commit конфиг с hooks для линтинга и форматирования.

## Spec

| Input | Processing | Output |
|-------|-----------|--------|
| git commit | lint + format + type-check | Commit accepted/rejected |

## Пошаговый план

1. Создать `.pre-commit-config.yaml`:

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/PyCQA/mypy
    rev: v1.14.0
    hooks:
      - id: mypy
        additional_dependencies:
          - types-requests
          - types-python-dotenv
          - pydantic>=2.0.0
        args: [--ignore-missing-imports]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.14.0
    hooks:
      - id: mypy
        files: "^bot/|^worker/"
```

2. Создать `ruff.toml`:

```toml
target-version = "py311"
line-length = 100

[lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "TCH",  # flake8-type-checking
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "B008",  # do not perform function calls in argument defaults
]

[lint.isort]
known-first-party = ["bot", "worker"]
```

3. Создать `.mypy.ini`:

```ini
[mypy]
python_version = 3.11
warn_return_any = True
warn_unused_configs = True
disallow_untyped_defs = False
disallow_incomplete_defs = False
check_untyped_defs = True
disallow_untyped_decorators = False
no_implicit_optional = True
strict_optional = True

[mypy-bot.*]
disallow_untyped_defs = True

[mypy-worker.*]
disallow_untyped_defs = True

[mypy-tests.*]
disallow_untyped_defs = False
```

4. Проверить: `pre-commit run --all-files`.

## Граничные случаи

- `pre-commit` не установлен — hook не запускается
- `ruff --fix` не может исправить — hook падает
- `mypy` находит type errors — hook падает
- Пустой проект — hooks должны работать без ошибок
- Файлы вне `bot/` и `worker/` — mypy игнорирует (`files: "^bot/|^worker/"`)

## Тесты (TDD)

- `tests/unit/test_precommit_config.py`:
  - `test_precommit_config_file_exists()` — `.pre-commit-config.yaml` существует
  - `test_precommit_has_ruff_hook()` — содержит `ruff`
  - `test_precommit_has_ruff_format_hook()` — содержит `ruff-format`
  - `test_precommit_has_mypy_hook()` — содержит `mypy`
  - `test_ruff_toml_file_exists()` — `ruff.toml` существует
  - `test_ruff_toml_target_version()` — `target-version = "py311"`
  - `test_ruff_toml_line_length()` — `line-length = 100`
  - `test_ruff_toml_selects_rules()` — содержит `E`, `F`, `I`, `N`
  - `test_mypy_ini_file_exists()` — `.mypy.ini` существует
  - `test_mypy_ini_python_version()` — `python_version = 3.11`
  - `test_mypy_ini_strict_optional()` — `strict_optional = True`
  - `test_mypy_ini_strict_bot()` — `disallow_untyped_defs = True` для `bot.*`
  - `test_mypy_ini_strict_worker()` — `disallow_untyped_defs = True` для `worker.*`

## Критерии приёмки

- [ ] `.pre-commit-config.yaml` существует
- [ ] Hook `ruff` с `args: [--fix]`
- [ ] Hook `ruff-format`
- [ ] Hook `mypy` с `additional_dependencies`
- [ ] `ruff.toml` существует с `target-version = "py311"`
- [ ] `ruff.toml` содержит `select: [E, W, F, I, N, UP, B, SIM, TCH]`
- [ ] `ruff.toml` содержит `known-first-party = ["bot", "worker"]`
- [ ] `.mypy.ini` существует с `python_version = 3.11`
- [ ] `.mypy.ini` содержит `strict_optional = True`
- [ ] `.mypy.ini` содержит `disallow_untyped_defs = True` для `bot.*` и `worker.*`
- [ ] `pre-commit run --all-files` проходит без ошибок (если pre-commit установлен)
- [ ] `pytest tests/unit/test_precommit_config.py` проходит (13 тестов)

## Зависимости

- **Зависит от:** T1.1.3 (.gitignore)
- **Блокирует:** T1.5.1 (CI pipeline)
